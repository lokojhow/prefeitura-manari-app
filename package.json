{
  "name": "prefeitura-manari-android",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicativo oficial da Prefeitura Municipal de Manari",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  }
}
